<%= form_for(@ride) do |f| %>
  <% if @ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride.errors.count, "error") %> prohibited this ride from being saved:</h2>

      <ul>
      <% @ride.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_lat %><br>
    <%= f.text_field :source_lat %>
  </div>
  <div class="field">
    <%= f.label :source_lon %><br>
    <%= f.text_field :source_lon %>
  </div>
  <div class="field">
    <%= f.label :dest_lat %><br>
    <%= f.text_field :dest_lat %>
  </div>
  <div class="field">
    <%= f.label :dest_lon %><br>
    <%= f.text_field :dest_lon %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
